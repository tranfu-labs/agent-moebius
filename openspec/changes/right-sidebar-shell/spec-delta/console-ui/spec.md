# console-ui delta：right-sidebar-shell

本 delta 为 `console-ui` 域新增右侧栏容器与标签条骨架的行为规格。合并时**新增** Requirement，不替换既有条目。

> 骨架文件。Requirement 由 codex 在 implement 段按本 change 负责的验收编号（#1 #2 #7 #15 #16 #17 #18 #19 #23）逐条写入，每条给出 `Source:` 锚点与可判定判据 + Scenario。此处不预写正文，避免面对全景只挑最像规格的少数几条落笔。
