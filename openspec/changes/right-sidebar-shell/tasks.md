# 任务：right-sidebar-shell

任务编号 `T\d+` 是 loop 调度器识别用的固定契约。`覆盖验收 #N` 指 `docs/product/pages/main-right-sidebar.md`「指标与验收」的编号。

前置：`main-conversation-evidence-outlets` 已落 main（`OperatorEvidenceOpenIntent` / `onOpenEvidence` / is-git 信号 / `SubSessionPanel` 均可复用）。

- [ ] T1: 标签模型与类型枚举
  - [ ] 定义标签类型枚举（改动 / 项目文件 / 过程 / 子任务 / 空白）与标签模型（含 sourceKey、可关闭、标题）
  - [ ] 显式区分「枚举全集」与「类型选择可见子集（仅改动 / 项目文件）」，两处不得混用
  - [ ] 覆盖验收 #17

- [ ] T2: 右侧栏容器与内容分派缝
  - [ ] `OperatorConsole` 的 `<main>` 同级挂右侧栏区，三栏并排 + 可拖拽调宽
  - [ ] 按 `tab.type` 分派到内容组件；改动 / 项目文件 / 过程 / 子任务四类给占位空槽（后续 change 替换）
  - [ ] `onOpenEvidence` 从「接 sub-session-panel 单视图」改为「打开 / 聚焦标签」；四类意图都能落成对应标签
  - [ ] 覆盖验收 #3（打开链路的骨架侧，内容由 ③ 补）

- [ ] T3: 开关与宽度持久化
  - [ ] 右侧栏默认关闭；显示 / 隐藏按钮在主内容区右上角，新对话无消息时也可用
  - [ ] 开关 + 宽度走 `right-sidebar-preference.ts`（全局键），跨对话切换 + 跨重启保留
  - [ ] 开合不影响会话区滚动位、草稿、正在进行的推进
  - [ ] 覆盖验收 #1

- [ ] T4: 标签条持久化与切换
  - [ ] 标签条（打开过哪些标签、选中哪个）走 `tabs:${sessionId}` 按对话键，跨重启恢复
  - [ ] 切换对话显示该对话自己的标签条；标签不跨对话携带
  - [ ] 反序列化容错：丢弃未知标签类型而非崩溃
  - [ ] 覆盖验收 #2

- [ ] T5: 加号、空白标签与类型选择
  - [ ] 加号新开空白标签（不去重）；空白标签是一句提问 + 两种类型（改动 / 项目文件）
  - [ ] 非 git 项目类型选择只剩「项目文件」并说明原因，不静默少一项
  - [ ] 空白标签说明「成员完整输出与子任务从主对话区点开」
  - [ ] 终端 / 预览 / 浏览器不得出现在类型选择里
  - [ ] 覆盖验收 #17 #18

- [ ] T6: 去重、关标签、焦点保护
  - [ ] 主对话区来源标签去重（聚焦已有）；加号标签不去重
  - [ ] 每个标签可关闭，关闭不影响对话与推进；关掉最后一个标签留一个空白标签、不收起右侧栏
  - [ ] 有新内容时不自动切换用户当前所在标签
  - [ ] 覆盖验收 #15 #16 #19

- [ ] T7: 窄窗覆盖
  - [ ] 窗口不足三栏时右侧栏覆盖会话区、给独立关闭按钮
  - [ ] 覆盖态提供回到会话区的方式；关闭后会话区回到原滚动位
  - [ ] 覆盖验收 #23

- [ ] T8: 真实桌面窗口验收
  - [ ] 从右上角按钮开合右侧栏 → 两栏 / 三栏切换，重启后开关与宽度仍在
  - [ ] 加号 → 空白标签给两种类型；非 git 项目只剩项目文件并说明
  - [ ] 开多个标签、关到最后一个 → 留空白标签、右侧栏不收起
  - [ ] 切换对话 → 标签条换成该对话的；重启应用 → 标签条恢复
  - [ ] 窄窗 → 覆盖会话区 + 独立关闭 + 回到会话区出口
