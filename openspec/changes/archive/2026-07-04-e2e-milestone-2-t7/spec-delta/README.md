# Spec delta

No behavior spec delta is planned for this change. T7 is a manual drill and roadmap record update; runtime behavior and the `github-issue-runner` spec remain unchanged unless the drill discovers a gap, in which case the gap is recorded as a future task rather than fixed here.
