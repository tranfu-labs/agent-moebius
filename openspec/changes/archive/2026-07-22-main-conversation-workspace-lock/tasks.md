# 任务：main-conversation-workspace-lock

任务编号 `T\d+` 是 loop 调度器识别 id 的固定契约。`覆盖验收 #N` 指 `docs/product/pages/main-conversation.md`「验收标准」的编号。

前置：`main-conversation-session-context` 已归档（本片拆的正是它交付的中途切换链路）。本片与 `main-conversation-evidence-outlets` 无依赖，可并行。

- [x] T1: 后端锁定判据
  - [x] `switchLocalSessionWorkspace` 在会话已有消息时拒绝，返回用户可理解的原因，不暴露列名或内部标识
  - [x] 判据以会话消息数为准，不信任前端传入的标志
  - [x] 覆盖验收 #5

- [x] T2: 拆除待生效工作空间机制
  - [x] 停止写入 `workspace_pending_mode`；保留列，不做删列迁移
  - [x] 读路径把存量残留值视作无效并按生效值解析，升级后不发生工作空间跳变
  - [x] `runtime` 步骤收尾的 pending 提升只处理团队
  - [x] 覆盖验收 #5

- [x] T3: 上下文条工作空间降级为锁定文本
  - [x] `composer-context.tsx` 在会话已有消息时把工作空间渲染为不可点击文本，与项目一致
  - [x] 待生效说明行只保留团队一路
  - [x] `operator-console.tsx` 收紧 `onChangeSessionWorkspace` 的传递条件
  - [x] 覆盖验收 #5

- [x] T4: 拆除切回默认路径与确认弹层
  - [x] 删除「换回默认工作空间」分支与整个工作空间确认弹层
  - [x] 产品内不留任何从独立切回默认的入口
  - [x] 覆盖验收 #5

- [x] T5: 新对话页承接工作空间选择与后果说明
  - [x] 新对话页提供工作空间选择（当前只有静态标签），否则「只在首条消息前可选」变成哪里都不可选
  - [x] 选择独立工作空间时说明副本基于项目当前所在的提交、不包含未提交改动
  - [x] 删除「此前已经在项目文件夹里产生的改动也不会被搬过去」——对话尚未开始，该前提不成立
  - [x] 非 Git 项目仍禁用独立工作空间并在同一菜单内说明原因
  - [x] 覆盖验收 #5 #8

- [x] T6: 旧语义清除与回归护栏
  - [x] `tests/local-console-pending-switch.test.ts` 的工作空间用例改写为「已有消息的会话拒绝切换」，不保留兼容分支
  - [x] 团队待生效用例原样保留并通过
  - [x] `composer-context.test.tsx`、`composer-context.stories.tsx` 随行为重写
  - [x] 回归：同项目两段对话仍可分别用默认与独立工作空间，互不影响
  - [x] 覆盖验收 #5 #7

- [x] T7: 真实桌面窗口验收
  - [x] 新对话页可选工作空间，选独立时看到后果说明
  - [x] 发出第一条消息 → 上下文条第二格变为不可点击文本，与项目表现一致
  - [x] 全页面找不到任何切回默认工作空间的入口，也不出现确认弹层
  - [x] 团队仍可改选，运行中改选仍在当前步跑完后生效
  - [x] 重启应用后既有会话的工作空间模式不变
  - [x] 覆盖验收 #5 #7 #8
