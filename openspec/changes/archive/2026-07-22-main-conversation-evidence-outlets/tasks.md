# 任务：main-conversation-evidence-outlets

任务编号 `T\d+` 是 loop 调度器识别 id 的固定契约。`覆盖验收 #N` 指 `docs/product/pages/main-conversation.md`「验收标准」的编号。

前置：`main-conversation-timeline-truth` 已归档（四种事实与状态点事实是本片复用的判据来源）。与 `main-conversation-workspace-lock` 无代码依赖，可并行；但产品逻辑上互为一端——基线只在工作空间全程不变时才有意义。

- [x] T1: 对话基线提交记入事实日志
  - [x] 会话创建（首条消息经 jsonl 事实写漏斗提交）时持久化「对话开始时项目所在的提交」，与工作空间模式同一时机
  - [x] 迁移幂等；既有会话无基线时降级为不可用而非报错
  - [x] 非 Git 项目不记录基线
  - [x] 覆盖验收 #22

- [x] T2: 对话级改动计数
  - [x] 默认工作空间统计项目文件夹相对基线的改动，含已提交与未提交两部分
  - [x] 独立工作空间以同一口径统计隔离副本
  - [x] 非 Git 项目返回不可用
  - [x] 不做改动归因，只给数量
  - [x] 新增 `tests/local-console-workspace-diff.test.ts`：两种模式口径 / 零改动 / 非 Git / 缺基线降级
  - [x] 覆盖验收 #22

- [x] T3: 计数随快照下发
  - [x] 计数与「是否可用」随会话快照下发，前端不自行推导
  - [x] 本片不新增改动清单路由——清单属右侧栏
  - [x] 覆盖验收 #22

- [x] T4: 结果卡片组件与出现判据
  - [x] 新增 `packages/console-ui/src/console/result-card.tsx`，含共置测试与 Story
  - [x] 只给数量与查看入口，不铺开文件清单
  - [x] 零改动如实说明，不省略
  - [x] 措辞不声称改动由团队成员造成
  - [x] 出现判据取「没有任何成员在工作且没有待处理的交棒」，复用状态点已有事实，不另起一套
  - [x] 非 Git 项目不出现卡片
  - [x] 覆盖验收 #22

- [x] T5: 完整输出入口
  - [x] `run-block.tsx` 操作条只提供「完整输出」，仍不显示计时；「停下」由运行中空草稿输入框的同一动作按钮承载
  - [x] 步骤结束后「完整输出」保留在历史记录条目上并可重新展开，历史记录不出现「停下」
  - [x] 四种事实的记录同样提供该入口
  - [x] `run-outcome.tsx` 恢复 `rawOutput` 出口，不再收成 `_rawOutput` 丢弃
  - [x] 覆盖验收 #11

- [x] T6: 打开意图与开发者诊断分离
  - [x] 新增独立的「打开右侧栏某类标签」意图，携带足以定位那一步的信息
  - [x] MUST NOT 复用 `onOpenDiagnostics`；「查看日志」仍只服务操作台自身故障
  - [x] 右侧栏就绪前，意图接现有 `sub-session-panel.tsx` 做单内容降级显示
  - [x] MUST NOT 长出标签条、类型选择、文件树或加号新建——那是右侧栏 PRD 的范围
  - [x] 覆盖验收 #11 #22

- [x] T7: 真实桌面窗口验收
  - [x] 跑一轮有改动 → 卡片出现，数字与实际改动文件数一致，点「查看」打开右侧区域
  - [x] 跑一轮零改动 → 卡片如实说明「什么都没改」
  - [x] 非 Git 项目跑一轮 → 不出现卡片
  - [x] 运行中记录看到「完整输出」、空草稿输入框看到唯一「停下」，无计时
  - [x] 步骤结束 → 历史记录不出现「停下」，「完整输出」仍在并可再次打开
  - [x] 一步没跑起来 → 该记录上也有「完整输出」
  - [x] 重启应用后卡片与入口仍在
  - [x] 覆盖验收 #11 #22
