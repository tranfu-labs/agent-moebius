# 任务：main-conversation-timeline-truth

任务编号 `T\d+` 是 loop 调度器识别 id 的固定契约。`覆盖验收 #N` 指 `docs/product/pages/main-conversation.md`「验收标准」的编号。

前置：`main-conversation-session-context` 已归档（T9「团队已删除」的恢复路径就是它交付的团队改选入口）。

- [x] T1: 系统记录事件类型落库
  - [x] `session_messages` 新增 `system_event_kind`，必填，覆盖四种事实 + 中性的「其他系统记录」
  - [x] 迁移：旧 `awaits_human_reason` 的 `exception` 归入中性类型且不触发红点，其余三种值丢弃
  - [x] runtime 每条写系统消息的路径都带 kind
  - [x] 新增 `tests/local-console-system-event-kind.test.ts`：每条路径都带 kind / 缺省为中性而非 null / 旧值迁移映射 / 迁移幂等
  - [x] 覆盖验收 #12

- [x] T2: 系统记录改用户语
  - [x] `runtime.ts` 中的英文机器串（跳过本地运行 / 未找到合法提及 / 子会话编排完成与失败）改成面向用户的中文表述
  - [x] 正文只承担人话，机器细节不进正文
  - [x] 覆盖验收 #13

- [x] T3: 机器信息过滤扩到所有渲染文本
  - [x] 新增 `packages/console-ui/src/console/machine-text.ts`，从 `operator-console.tsx` 迁出既有正则
  - [x] 过滤范围覆盖 agent 消息体、run step 标题、系统记录
  - [x] 项目修复对话框中用户自选的文件夹路径回显不在过滤范围内
  - [x] 新增 `packages/console-ui/src/console/machine-text.test.ts`：路径 / cwd / runDir / 内部 id 被挡；正常中文文案不被误伤
  - [x] 覆盖验收 #13

- [x] T4: 收掉过程状态标记与计时
  - [x] `run-block.tsx` 删除步骤状态标签、状态图标与 `elapsed` 计时；只留实时输出与末尾「停下」
  - [x] 这一步结束后操作条消失，历史记录中不留痕迹
  - [x] `session-context-header.tsx` 删除「通过 / 运行中 / 等你」计数条
  - [x] `badge.tsx` 的过程态变体收敛，移除时间线不再使用的档位
  - [x] 时间只在悬停时显示
  - [x] 覆盖验收 #10 #11

- [x] T5: 四种事实的表现与可用操作
  - [x] `run-outcome.tsx` 按 `system_event_kind` 渲染四种事实，彼此可区分
  - [x] 没跑起来、卡住提供「重试」；已停下、反复重试仍未成功不提供
  - [x] 「已停下」为中性表述，不写成失败；停下之后可以继续说话，开启新的一轮
  - [x] 「停下」中止当前这一步，已经产生的文件改动不会被回滚，界面不得暗示可以撤销
  - [x] 用户也可以不重试，直接说话或换一个成员接手
  - [x] 文案说「这一步没跑起来」，不说「开发失败了」
  - [x] 刷新页面或重启应用后四种事实仍然可见
  - [x] 覆盖验收 #12

- [x] T6: 状态点判据换源
  - [x] 新增 `packages/console-ui/src/console/status-dot.ts`，`deriveStatusDot` 从 `conversation-sidebar.tsx` 迁出
  - [x] 红=存在未处理的没跑起来 / 卡住 / 反复重试仍未成功，或处于三种不可继续状态之一
  - [x] 蓝=没有成员在工作、最后一条消息未提及任何人、且用户尚未查看这些新结果；保留既有的「打开并展示最新结果后清除」机制
  - [x] 闪=有成员在工作
  - [x] 用户按了停不触发红点；最后一条提及了成员时既不红也不蓝；正常完成不产生红点
  - [x] 三种不可继续状态同时触发红点与只读表现，两者不互斥
  - [x] 优先级 红 > 蓝 > 闪 > 无 保持；辅助名称不依赖颜色即可读懂
  - [x] MUST NOT 保留「等人回话」作为第四种红点来源
  - [x] 新增 `packages/console-ui/src/console/status-dot.test.ts`：逐条覆盖上述判据与三条豁免、项目行聚合
  - [x] 覆盖验收 #16

- [x] T7: 红点必有可读记录的闭环校验
  - [x] 不存在任何不落系统消息就置红的路径
  - [x] 单测断言：置红的每种来源都对应一条带 kind 的系统消息
  - [x] 覆盖验收 #16

- [x] T8: 三种不可继续状态统一判定
  - [x] 新增 `src/local-console/session-status.ts`：产出能不能继续 / 为什么不能 / 恢复动作
  - [x] 「团队已删除」从「需要修复」中拆出为独立状态
  - [x] 三种情况共用同一种表现：历史只读、输入与发送禁用、对应上下文按钮标红；区别只在说明文案与恢复路径
  - [x] 新增 `tests/local-console-session-status.test.ts`：三种判定 / 各自恢复条件 / 团队已删除与需要修复可区分
  - [x] 覆盖验收 #17

- [x] T9: 三种恢复路径接通
  - [x] 项目文件夹修复成功 → 恢复输入、发送和推进，历史与选择不变
  - [x] 团队被删除后改选另一支可用团队 → 恢复推进，历史保留，新团队从当前上下文接手
  - [x] 团队修复完成 → 自动恢复推进能力，不需要用户再操作一次
  - [x] 覆盖验收 #17

- [x] T10: 运行期分流纳入团队健康
  - [x] 三种情况在有成员正在工作时发生：已有有效隔离副本、能安全完成的执行跑完当前这一步；依赖已不可用的项目文件夹或团队内容、无法安全继续的立即停止并留下可读记录
  - [x] 执行已经无法继续时，界面不再显示成员正在工作
  - [x] 覆盖验收 #18

- [x] T11: 收紧绑定团队名单并保留存量兼容
  - [x] 已绑定会话只从团队快照取 Agent，主 Agent 排在首位
  - [x] 已绑定团队删除 / 需修复时显式阻断，不回退共享 `agents/`
  - [x] 未绑定的存量会话仍回退共享 `agents/`，不误判成团队已删除
  - [x] `src/local-console/server.ts` 的默认名单解析保留同一存量兼容语义
  - [x] 覆盖验收 #14（Agent 只来自团队部分）

- [x] T12: 提及语义核对
  - [x] 用户不提及任何人时消息发给当前团队的主 Agent
  - [x] 提及某个正在工作的成员会让它停下当前这一步，带着新的指令重新开始
  - [x] 主 Agent 转达用户的话时具备同样的效果
  - [x] 覆盖验收 #14

- [x] T13: 真实桌面窗口验收
  - [x] 制造一次失败运行 → 「这一步没跑起来」+ 重试 + 侧边栏红点
  - [x] 按停 → 中性记录，无红点
  - [x] 重启应用 → 四种事实仍可见且彼此可区分
  - [x] 通读时间线 → 无过程状态标记、无计时、无路径与内部 id
  - [x] 删掉当前团队 → 只读态 + 改选团队后恢复推进，历史保留
  - [x] 把项目文件夹移走（有成员正在工作时）→ 隔离工作区那条跑完当前步再停，直接改文件那条立即停并留下可读记录；界面不再显示成员正在工作
  - [x] 覆盖验收 #10 #11 #12 #13 #16 #17 #18
