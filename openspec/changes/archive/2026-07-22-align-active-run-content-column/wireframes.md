# 线框：align-active-run-content-column

> 目标字符图已回流至 `docs/product/pages/main-conversation.md#页面结构`。

## pages/console.md

基线：`docs/wireframes/pages/console.md`。该文件已注明新会话主页面由 `docs/product/pages/main-conversation.md` 接管；本线框沿用其主时间线骨架，并以 PRD 为冲突时的上位事实源。

### 当前

```text
760px 时间线容器
┌────────────────────────────────────────────────────────────┐
│    会话标题 / 历史消息正文                                 │
│技术负责人                               [□ 停下]           │
│实时 Markdown                                                │
│<──────────── 运行块只有 680px ────────────>│                │
└────────────────────────────────────────────────────────────┘
 ↑ 运行态向左偏 40px；右边界也短于正文列
```

### 目标

```text
760px 时间线容器
┌────────────────────────────────────────────────────────────┐
│    会话标题 / 历史消息正文                                 │
│    技术负责人                              [□ 停下]        │
│    实时 Markdown                                           │
│    <────────────── 720px 正文列 ──────────────>             │
└────────────────────────────────────────────────────────────┘
     ↑ 左边界一致                         右边界一致 ↑
```

窄窗口时 720px 不是固定宽度；正文列等于「时间线可用宽度减去 40px 左缩进」，活动运行块随该列自然缩小。
