# 任务：fix-final-reflection-template

- [x] 修改 `src/triggers/reflector-stage-trigger.ts`，让最后一次自动反思 hook 追加收敛指令。
- [x] 更新 `tests/triggers.test.ts`，覆盖最后一次 hook 模板与达到上限后跳过。
- [x] 更新 `agents/reflector.md` 与 `AGENTS.md` 中的反思机制说明。
- [x] 运行 `pnpm test`、`pnpm typecheck` 与 `git diff --check`。
