# 任务：right-sidebar-change-project-files

前置：`right-sidebar-shell` 已落 main（提供改动 / 项目文件两类内容标签的占位槽与分派缝）；`main-conversation-evidence-outlets` 已落 main（`baselineCommit` + `workspace-diff.ts` diff 生成 + `workspace-diff` 入口意图）。

- [x] T1: 改动清单读取后端（清单 + 增删行数）
  - [x] 扩 `workspace-diff.ts`：在既有 diff 上补 `--numstat`，产出「改动文件清单 + 每文件增删行数」，覆盖项目文件夹 / 独立工作空间两种模式，非 git 返回不可用
  - [x] 共享语义回扫：不破坏 evidence-outlets 对 fileCount 的既有用途，callsite_sweep 记全调用点
  - [x] 新增只读 HTTP 路由下发清单（对照 evidence-outlets 的 `/runs/:runId/output` 范式）
  - [x] 覆盖验收 #9 #21（增删行数）

- [x] T2: 行级 hunk 读取后端
  - [x] 解析 `git diff` patch → 逐行标新增 / 删除 / 未改动，下发选中文件的行级内容
  - [x] 覆盖验收 #8

- [x] T3: 项目文件读取后端（文件树 + 内容）
  - [x] 列完整项目文件树 + 读任意文件内容的只读通道
  - [x] 文件过大 / 非文本时返回可显示的原因，不静默留白
  - [x] 有改动的文件同样能取到行级变化
  - [x] 覆盖验收 #23（页面状态：项目文件 / 读取失败）

- [x] T4: 文件内容呈现组件（改动 / 项目文件共用）
  - [x] `file-diff-view.tsx`：行级新增 / 删除 / 未改动，未改动行照常显示提供上下文
  - [x] 长行不换行、横向滚动、不撑宽页面；行号列横滚时保持可见
  - [x] 文件树 / 文件内容各自独立滚动；内容可选中复制
  - [x] 覆盖验收 #8 #22

- [x] T5: 改动标签
  - [x] 顶部一句话说明「这段对话期间项目发生了这些改动」
  - [x] 说明看的是项目文件夹还是独立工作空间；独立工作空间追加「改动在隔离副本里、你的项目文件夹没被动过」
  - [x] 说明文案不声称改动来自成员（不归因）
  - [x] 文件树只列改动文件；累计改动基于对话开始基线、非最后一步
  - [x] 三种空态措辞互不相同（对话没开始 / 跑过没改动 / 读取中）
  - [x] 团队工作时说明「列表截至上一轮结束」+ 手动刷新；不实时跟随
  - [x] 非 git 项目不提供改动标签（结果卡片 / 加号选改动都被 is-git 守卫拦下）
  - [x] 覆盖验收 #3 #4 #5 #6 #9 #10 #11

- [x] T6: 项目文件标签
  - [x] 完整文件树 + 文件内容，含未改动文件；有改动文件同样标出变化
  - [x] 同样说明看的是项目文件夹还是独立工作空间
  - [x] 覆盖验收 #9（浏览未改动走此类型）

- [x] T7: 选文件与刷新态
  - [x] 点文件在下方显示内容，当前选中文件在树中保持可见；切换文件不改变 / 不新建标签
  - [x] 阅读文件时刷新不跳走、不丢位置；有新改动给可点击提示由用户决定何时看
  - [x] 全片只读：无编辑 / 保存 / 撤销 / 还原 / git 动作控件与文案
  - [x] 覆盖验收 #20 #21（只读）

- [x] T8: 真实桌面窗口验收
  - [x] 跑一轮有改动 → 结果卡片 [查看] 打开改动标签，文件树数量与实际一致，点文件看行级对比
  - [x] 独立工作空间模式 → 说明「隔离副本、项目文件夹没被动过」
  - [x] 项目文件夹模式 → 说明看的是项目文件夹；累计改动含全程非最后一步
  - [x] 非 git 项目 → 无改动标签
  - [x] 加号选项目文件 → 浏览全树 + 未改动文件内容
  - [x] 团队工作中 → 改动标签说明「截至上一轮」+ 刷新可用；阅读中刷新不丢位置
  - [x] 全区找不到任何编辑 / 还原 / git 控件；内容可选中复制
