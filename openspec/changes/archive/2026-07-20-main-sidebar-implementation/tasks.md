# 任务：main-sidebar-implementation

任务编号 `T\d+` 是 loop 调度器识别 id 的固定契约。子勾选项由承接任务的 codex 完成时逐条打勾；`覆盖验收 #N` 指 `docs/product/pages/main-sidebar.md` 的验收标准编号。

- [x] T1: 侧边栏骨架与固定布局
  - [x] `operator-console.tsx` 顶部固定品牌区（Logo + `Moebius`）、macOS 交通灯避让与拖拽区
  - [x] 顶部固定「关闭侧边栏」按钮，含辅助名称
  - [x] 顶部固定三个应用级入口：`＋ 新建对话`、`⌕ 搜索`、`◇ Agent 团队`；同高度、同视觉层级
  - [x] `项目` 区标题；中部项目列表是唯一可滚动区域
  - [x] 底部固定「⚙ 设置」入口；移除当前占位的「开发者诊断」+ 引擎状态常驻
  - [x] 覆盖验收 #1 #15

- [x] T2: 拆除旧状态排序与已完成折叠、对话按创建时间倒序
  - [x] 移除 `sortConversationSessions` 与 `statusOrder`
  - [x] 移除 `openCompleted`、`toggleCompleted`、"已完成"分组 UI 与相关测试
  - [x] 侧栏渲染改为按 session 稳定 `createdAt` DESC；新建 session 后立即出现在最上方
  - [x] session 数据层确保有稳定 `createdAt`（不存在则从最早消息 id 推导并回填）
  - [x] 覆盖验收 #6 #7 前半

- [x] T3: 会话状态模型改为 red/blue/blink/none
  - [x] 数据层新增 `awaits_human_reason`（enum）与 `unread_since`（timestamp）
  - [x] agent 回复插入时 set `unread_since`，用户打开该会话且展示最新结果后清除
  - [x] `ConversationSessionStatus` 与 `statusLabel` / `StatusIcon` 用四态点替换：red（需要人工处理）/ blue（未读结果）/ blink（运行中）/ none
  - [x] 派生规则集中在 `deriveStatusDot(session)`，优先级 red > blue > blink > none
  - [x] 辅助名称必须能不依赖颜色读懂：`需要你处理` / `有新结果` / `正在运行` / 无
  - [x] 覆盖验收 #5

- [x] T4: 侧边栏关闭 / 恢复 / 状态记忆
  - [x] 侧栏关闭按钮真接线；主内容区扩宽
  - [x] 主内容区顶部固定「打开侧边栏」真按钮，取代当前占位装饰
  - [x] 关闭/打开不触发主区时间线或运行块的重挂载（增回归测试）
  - [x] 记住当前对话、项目展开状态、项目列表滚动位置
  - [x] localStorage 记住最后一次开合选择；首次启动引导期间强制可见
  - [x] 覆盖验收 #2 #16 记忆部分

- [x] T5: 项目展开 / 折叠 + 折叠时状态聚合
  - [x] 项目行支持展开箭头 + 点击项目名切换展开
  - [x] 折叠时用 red > blue > blink 派生一个聚合点；展开时项目行不重复展示状态点
  - [x] 当前对话所在项目允许手动折叠；主内容区继续显示，不自动重新展开
  - [x] 项目行 `＋` 和 `⋯` 按钮 stopPropagation，不触发行事件
  - [x] 覆盖验收 #5 折叠部分

- [x] T6: 应用级入口路由（新建对话 / 搜索 / Agent 团队）
  - [x] 三个按钮接线到对应目标（`new-conversation.md` / `search.md` 的窗口不存在时打开占位模态并保留入口）
  - [x] Agent 团队入口接线到 stub 页面，保留当前选中态
  - [x] 视觉层级三者一致，不因 Agent 团队特殊化
  - [x] 覆盖验收 #3 #12

- [x] T7: 项目内新建对话预选项目
  - [x] 项目行加号点击 → 打开新建对话窗口（同 T6 的占位）并预选该项目
  - [x] 点击瞬间不创建持久化空白会话
  - [x] 项目目录失效或项目正在变更时按钮 disabled 并说明
  - [x] 覆盖验收 #4

- [x] T8: 项目行菜单（显示名称 / 文件管理器 / 移除）
  - [x] `⋯` 菜单：`在文件管理器中显示` / `修改显示名称` / `移除项目`
  - [x] 修改显示名称弹层；空名称回退为文件夹名
  - [x] 移除项目二次确认弹窗，明确不删除磁盘文件夹
  - [x] 项目有运行中 agent 时先弹独立警告，允许取消或强制中止后移除
  - [x] 移除当前项目后主内容区进入"新建对话且项目未选"状态
  - [x] IPC：`showInFolder(path)` 走 desktop preload
  - [x] 覆盖验收 #11

- [x] T9: 项目拖动排序 + 持久化 + 新项目置顶
  - [x] 项目行本身即拖动载体，无独立把手
  - [x] 5px + 150ms 阈值区分拖动与展开点击
  - [x] 拖动结束不触发展开或折叠
  - [x] 顺序持久化到 store；重启保持
  - [x] 新添加项目默认置顶并自动展开、成为当前项目
  - [x] 覆盖验收 #10

- [x] T10: 项目目录不可用 + 扳手修复
  - [x] 目录不可用时项目行显示独立红色扳手按钮，含悬停提示
  - [x] 修复入口不进 `⋯` 菜单
  - [x] 点击扳手 → 系统文件夹选择器 → 校验"一个文件夹只能绑定一个活动项目" → 更新记录
  - [x] 确认前显示原位置与新位置对比
  - [x] 目录失效期间禁止新建对话、发送消息、切换团队、启动执行；历史对话只读
  - [x] Agent 运行期间目录失效的降级：有 worktree 的完成当前一步后停止，无 worktree 的立即停止并转红点
  - [x] IPC：`selectFolderForRepair(projectId)` 走 desktop preload
  - [x] 覆盖验收 #9

- [x] T11: 对话归档 + 运行中禁止归档
  - [x] 对话行悬停/键盘聚焦/上下文菜单提供「归档」
  - [x] 数据层 `archived_at` 字段；运行中的会话 archive API 拒绝
  - [x] 归档立即清 active_run 并停止 T4.5 handoff drain；恢复归档时按位点续跑
  - [x] 归档当前对话 → 选择同项目相邻可见对话；无则显示项目空态
  - [x] 覆盖验收 #7 后半

- [x] T12: 空态 / 加载 / 失败 / 无项目状态
  - [x] 无项目：项目区显示"从'新建对话'添加第一个项目"，顶部三入口仍可点
  - [x] 项目无对话：展开后显示"还没有对话"
  - [x] 数据加载中：项目区显示保持结构的加载占位
  - [x] 加载失败：项目区显示简短错误 + 重试；Agent 团队/设置/重试可用
  - [x] 应用级配置变更中：只 disable 冲突入口，无关浏览不阻断
  - [x] 覆盖验收 页面状态表

- [x] T13: 宽度拖动 + 窄窗自动关闭
  - [x] 侧栏右侧分隔边界可拖动；宽度上下限内可用
  - [x] 长名称仍单行截断，悬停显示完整
  - [x] 窗口宽度低于阈值自动关闭侧栏；窗口重新变宽后按用户最近一次选择决定
  - [x] 覆盖验收 #14 #16 宽度部分

- [x] T14: 键盘可达性 + 悬停提示 + 状态点辅助名称收尾
  - [x] 所有图标入口可 Tab 聚焦，有可读辅助名称
  - [x] 长项目名 / 长对话名 title 属性
  - [x] 补齐红/蓝/闪烁/无点的 `aria-label` 与 `title`
  - [ ] 全面走查 17 条验收，逐条留存证据
  - [x] 覆盖验收 #17 + 全量回归
