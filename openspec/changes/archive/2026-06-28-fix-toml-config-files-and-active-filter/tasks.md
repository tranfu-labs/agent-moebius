# 任务：fix-toml-config-files-and-active-filter

- [x] 新增提交版 `config.toml` 示例文件。
- [x] 把本机覆盖文件改为 `config.local.toml` 并更新 `.gitignore`。
- [x] 调整 local config 解析，允许纯注释配置并支持 default + local override。
- [x] 调整 active issue due 和 active limit，只作用于当前 watched repositories。
- [x] 更新 AGENTS、模块地图和 github-issue-runner spec。
- [x] 补充 local config 和 intake 测试。
- [x] 运行 `pnpm test`。
- [x] 运行 `pnpm typecheck`。
