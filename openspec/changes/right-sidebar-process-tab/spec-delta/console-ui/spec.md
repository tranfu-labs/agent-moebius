# console-ui delta：right-sidebar-process-tab

本 delta 为 `console-ui` 域新增过程标签渲染（完整输出、多次执行分段、标题成员名、截断可见、只读可复制）的行为规格。**新增** Requirement，不替换既有条目。

> 骨架文件。Requirement 由 codex 在 implement 段按 UI 相关验收（#12 #13 #14 #22）逐条写入，含 `Source:` 锚点、可判定判据、Scenario。此处不预写正文。
