# 任务：right-sidebar-process-tab

前置：`right-sidebar-shell` 已落 main（提供过程内容标签的占位槽与分派缝）；`main-conversation-evidence-outlets` 已落 main（`run-output` 意图 + `runtime.runOutput()` + `/runs/:runId/output` 路由）。

- [ ] T1: 多次执行聚合后端
  - [ ] 按 (成员, 步骤) 聚合同一步骤的多个 run 输出为有序分段（第 1 次 / 第 2 次…）
  - [ ] 扩 / 新增聚合入口于 `runtime.ts`；共享语义回扫：不破坏 evidence-outlets 对 `runOutput` 的既有用途
  - [ ] runDir 缺失（`/tmp` 被清）时降级到 `fallback` 并可标出「原始输出不可用」
  - [ ] 覆盖验收 #14

- [ ] T2: 截断可见信号
  - [ ] 把留存侧的截断诊断转成可下发的可见标记（不再是机器串）
  - [ ] 下发类型 / 路由（对照 evidence-outlets 的 output 路由）
  - [ ] 覆盖验收 #12（已知隐患：截断可见）

- [ ] T3: 过程标签渲染
  - [ ] 消费聚合输出，含原始错误；按时间顺序追加，不摘要、不折叠、不美化改写（不走 run-block markdown 通道）
  - [ ] 正在工作实时追加；结束后定格、标签继续可看
  - [ ] 同步骤多次执行依次可见并标明第几次，不覆盖
  - [ ] 输出被截断时显示可见「此处已截断」标记
  - [ ] 覆盖验收 #12 #14

- [ ] T4: 标题 = 成员名 + 序号
  - [ ] 标题取意图 `role` → 既有 role→名映射；取不到给「成员未知」，不自编描述
  - [ ] 同一成员这段对话内第二个及以后过程标签追加序号（开发 / 开发 2 / 开发 3）
  - [ ] 标题过长截断、悬停显示完整
  - [ ] 覆盖验收 #13

- [ ] T5: 只读可复制
  - [ ] 过程标签只读；内容与文件路径可选中复制
  - [ ] 原始输出通道绕开 `sanitizeMachineText`，保留原始路径
  - [ ] runDir 缺失 / 输出为空用不同措辞说明，不静默留白
  - [ ] 覆盖验收 #22 #21（过程标签只读）

- [ ] T6: 真实桌面窗口验收
  - [ ] 点某成员某一步「完整输出」→ 过程标签打开，含原始错误的完整输出
  - [ ] 标题是成员名；同成员第二个过程标签带序号
  - [ ] 让一步失败并重试 → 同一标签内两次执行依次可见、标第几次
  - [ ] 制造超大输出 → 显示「已截断」标记，不假装完整
  - [ ] 内容与路径可选中复制；找不到任何编辑 / 写操作
  - [ ] 重启应用后老步骤原始输出读不到时明说「不可用」，不留白
