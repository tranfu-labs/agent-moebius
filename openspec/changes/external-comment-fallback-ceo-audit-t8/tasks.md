# Tasks: external-comment-fallback-ceo-audit-t8

- [x] Reconstruct and update the OpenSpec change in the current worktree.
- [x] Record issue 41 contradictory conclusion evidence classification and scope trimming in `design.md`.
- [x] Add fallback route decision state types, pure helpers, and old-state compatibility validation.
- [x] Add stateless fallback route parsing / validation in the CEO guardrail area.
- [x] Wire active-only no-mention external comment fallback routing in `processIssueSource`.
- [x] Add uniform `ceo-reviewed` metadata helpers and apply them to all runner publishing paths.
- [x] Update `agents/ceo.md` with fallback route judgment criteria and output contract.
- [x] Add / update unit tests for intake state, route parsing, runner fallback routing, publishing metadata, and speaker normalization.
- [x] Add route timeout / never-settling fake promise tests proving bounded `fail_open` and non-blocked follow-up heartbeat behavior.
- [x] Add inline-code-only fallback append mention parser coverage.
- [x] Run `git diff --check`, `pnpm test`, and `pnpm typecheck`.
- [x] Review implementation against this change before emitting `code-verified`.
- [x] After accepted implementation evidence, update `docs/roadmap/milestone-2-stability-oracle.md` under T8 and check the task.
