# 任务：agent-teams-implementation

事实源：`docs/product/pages/agent-teams.md`。「覆盖验收 #N」指该文档「指标与验收」小节的编号。

- [ ] T1: 团队与成员数据模型 + 磁盘布局
  - [ ] 定义 `<dataRoot>/teams/` 布局：`.system/` 内置区与 `<user-team-id>/` 用户区分离
  - [ ] 定义 `team.json`（团队名称、一句话描述、主 Agent slug、成员顺序）与 `members/<slug>/AGENT.md`
  - [ ] 从 `AGENT.md` 解析显示名称与一句话描述，不维护第二份成员摘要
  - [ ] 团队归属判定：内置区只读，用户区可写；写入口按归属拒绝而非依赖 UI 禁用
  - [ ] 结构有效性判定：恰有一名主 Agent、主 Agent 是当前成员、slug 团队内唯一、全部 `AGENT.md` 可读
  - [ ] 有效性结果映射到状态：可用 / 未完成草稿 / 需要修复
  - [ ] 覆盖验收 #4 #6 #9
  - 依赖: 无

- [ ] T2: 内置团队种子打包与升级覆盖播种
  - [ ] 新增仓库 `seeds/teams/`，含一支内置团队：名为「开发团队」，成员为开发经理、开发、软件测试，主 Agent 为开发经理
  - [ ] 每名成员的 `AGENT.md` 只写显示名称标题加一行职责描述，不写长篇设定
  - [ ] `desktop/package.json` 的 `extraResources` 新增 `seeds/teams` → `seed/teams`
  - [ ] 计算 `seed/teams` 内容指纹，与 `<dataRoot>/teams/.system/.teams-seed.marker` 比对
  - [ ] 指纹不匹配时解包到临时目录后原子替换 `.system/`，成功后最后写入指纹文件
  - [ ] 指纹匹配时整个跳过；用户区在任何情况下不被触碰
  - [ ] 与现有 `buildSeedCopyPlan` 的「存在即跳过」并存，不改动 `agents/` 与 `config.toml` 的既有行为
  - [ ] 上一版存在、本版移除的内置团队随覆盖消失，引用它的记录回退
  - [ ] 覆盖验收 #3
  - 依赖: T1

- [ ] T3: IPC 通道 + 团队页数据容器 + 加载态
  - [ ] `desktop/src/main.ts` 与 `preload.ts` 暴露团队列表与成员读写 IPC
  - [ ] `desktop/src/console-page/app.tsx` 载入团队数据并维护选择态
  - [ ] `operator-console.tsx` 的 `agent-teams` 占位视图替换为接真实数据的容器
  - [ ] 页面加载中：使用保持团队横行结构的加载占位
  - [ ] 页面加载失败：保留页面框架，显示错误和重试，不把失败误判为没有团队
  - [ ] 内置团队也无法加载时显示应用配置异常，不引导用户当作空白重建
  - [ ] 覆盖验收 #1
  - 依赖: T2

- [ ] T4: 团队首页横行陈列
  - [ ] 一支团队一条大横行：名称、一句话描述、状态徽标、成员数与主 Agent 名、扁平成员示意、整行点击进入详情
  - [ ] 「内置」「未完成」「需要修复」徽标
  - [ ] 扁平表达：无连接线、箭头、环形轨道、中心节点、树形缩进；成员同尺寸同视觉权重
  - [ ] 主 Agent 只通过排第一位与「主 Agent」标签表达，其他成员不表达顺序
  - [ ] 空间不足时显示前几名成员与 `＋N`
  - [ ] 窄窗口保持一队一横行不改多列网格，成员行移到描述下方
  - [ ] 横行不常驻修改、复制、删除按钮
  - [ ] 从详情返回时保留此前滚动位置
  - [ ] 覆盖验收 #1 #2
  - 依赖: T3

- [ ] T5: 团队详情骨架 + 成员选择器 + `AGENT.md` 编辑区
  - [ ] 顶部团队身份区、中部单行横向滚动成员选择器、下方全宽 `AGENT.md` 编辑器
  - [ ] 成员选择器同时是选择器与展示区，点击只切换下方编辑对象，不进入独立 Agent 页面
  - [ ] 多名成员可同时保留未保存草稿；切换成员不弹窗、不丢草稿
  - [ ] 当前成员显示未保存标记；`AGENT.md` 不自动保存
  - [ ] 「保存」只保存当前成员；支持 `Command/Ctrl + S`
  - [ ] 正在保存时显示进度并防止重复保存；保存成功后清除未保存标记
  - [ ] 保存失败保留编辑内容，在当前编辑区显示原因和重试
  - [ ] 离开团队详情时若仍有未保存内容，提供「继续编辑」「放弃全部」「保存全部并离开」
  - [ ] 「保存全部并离开」逐个保存，部分失败时成功项保持已保存、失败项保留草稿、停留在当前详情并显示失败成员列表，不伪装整体成功也不回滚已成功项
  - [ ] 窗口缩放、成员切换、横向滚动不丢失任何成员的草稿
  - [ ] 覆盖验收 #5
  - 依赖: T3

- [ ] T6: 主 Agent 选择器与切换规则
  - [ ] 用户团队详情顶部常驻主 Agent 选择器，不藏进「修改信息」或「更多」
  - [ ] 只能选择当前团队的现有有效成员
  - [ ] 切换成功后立即保存并给出轻量反馈，不进入额外编辑模式
  - [ ] 新主 Agent 自动移动到成员行第一位
  - [ ] 切换只影响之后使用该团队创建的会话，已有会话继续使用创建时载入的版本
  - [ ] 内置团队不能切换主 Agent
  - [ ] 覆盖验收 #4
  - 依赖: T5

- [ ] T7: 新建团队草稿 + 添加 Agent
  - [ ] 「新建团队」弹层只收集团队名称与一句话描述，不塞入成员编辑
  - [ ] 创建后进入可长期保留的未完成草稿，显示在首页并标记「未完成」
  - [ ] 草稿空态：还没有团队成员 + 添加第一个 Agent 的入口
  - [ ] 「添加 Agent」在当前详情内切换到新成员编辑状态
  - [ ] 新成员取得创建后不再改变的团队内唯一稳定 slug，并写入含可编辑显示名称与一句话描述的初始 `AGENT.md`
  - [ ] 第一个成功添加的 Agent 自动成为主 Agent
  - [ ] 没有主 Agent 的草稿不能用于新建对话；单成员团队可用
  - [ ] 用户团队「修改信息」只改团队名称与一句话描述
  - [ ] 覆盖验收 #4
  - 依赖: T6

- [ ] T8: 内置团队只读 + 复制并编辑
  - [ ] 内置团队详情沿用同一信息层级，但团队信息、主 Agent、成员构成与 `AGENT.md` 全部只读
  - [ ] 不提供修改入口、不提供删除、不提供复制单个成员
  - [ ] 「复制并编辑」复制团队对应的完整目录（团队信息、全部成员目录、各成员 `AGENT.md` 及其他相关文件）为独立用户团队并进入其详情
  - [ ] 复制不改变新建对话的「上一次使用」记录
  - [ ] 绕过 UI 直接调用写接口修改内置团队必须被拒绝
  - [ ] 通过外部工具修改内置文件不使内置团队变成用户团队
  - [ ] 覆盖验收 #3
  - 依赖: T5

- [ ] T9: `@slug` 提及与自动补全
  - [ ] 在软件内编辑 `AGENT.md` 时输入 `@` 匹配当前团队成员，结果同时显示可读名称与 `@<slug>`
  - [ ] 选中后编辑器显示以可读名称为主的提及组件，slug 不成为正文主要视觉噪声
  - [ ] 底层始终保存 `@<slug>`；外部编辑器打开看到的是稳定 slug 文本
  - [ ] 提及组件悬停或获得键盘焦点时可查看并复制底层 `@<slug>`
  - [ ] 显示名称改变后既有提及组件自动显示新名称，底层 slug 与引用关系不变
  - [ ] 自动补全不从团队外引入成员，不验证自然语言交棒是否合理
  - [ ] 成员信息区持续显示并允许复制 `@<slug>`
  - [ ] 覆盖验收 #6
  - 依赖: T5

- [ ] T10: Finder / 平台文件管理器入口
  - [ ] 团队「更多」菜单的「在 Finder 中打开」打开整支团队对应位置
  - [ ] 当前成员菜单的同名操作打开当前成员对应位置
  - [ ] Windows 与 Linux 使用各自平台的文件管理器名称与行为
  - [ ] 作为辅助入口，不作为页面主操作常驻展示
  - [ ] 内置团队的入口只用于查看软件自带内容
  - [ ] 覆盖验收 #7
  - 依赖: T5

- [ ] T11: 复制 / 删除 Agent 与删除用户团队
  - [ ] 用户团队成员菜单支持复制 Agent：复制该成员整个目录与全部相关文件，复制品仍属当前团队并取得新的团队内稳定 slug
  - [ ] 删除 Agent 前提示其他成员的交棒规则可能仍引用该成员，产品不会自动改写；确认后仍可删除
  - [ ] 删除当前主 Agent 前必须先指定另一名有效成员
  - [ ] 删除成员后若剩余结构仍满足可用条件，团队不因此进入「需要修复」
  - [ ] 用户团队可从「更多」菜单复制
  - [ ] 用户团队与未完成草稿可从详情「更多」移到系统废纸篓或回收站；内置团队不显示删除能力
  - [ ] 删除前说明团队包含的 Agent 与相关文件并要求确认
  - [ ] 存在未保存草稿时，复制与删除都必须先让用户保存或放弃；复制始终基于已完整落盘的文件
  - [ ] 删除成功后返回团队首页并从新建对话团队选择器中移除
  - [ ] 已有会话及其创建时载入的团队版本不因删除而移除
  - [ ] 不提供软件内永久删除与独立的已删除团队页面
  - [ ] 覆盖验收 #11
  - 依赖: T6, T8

- [ ] T12: 外部修改冲突
  - [ ] 只对可编辑的用户团队生效；内置团队不能通过外部修改进入可编辑或已更新状态
  - [ ] 软件内无未保存内容时，返回应用后重新载入外部更新的 `AGENT.md` 并给出轻量提示
  - [ ] 软件内已有未保存草稿时不自动覆盖，保留草稿并提示文件已在外部修改
  - [ ] 只提供「载入外部版本」与「用当前内容覆盖」两个选择，不提供逐行 diff 或自动合并
  - [ ] 不得静默选择任何一方覆盖
  - [ ] 其他引用文件的外部修改不触发本页刷新或冲突提示
  - [ ] 覆盖验收 #5
  - 依赖: T5

- [ ] T13: 需要修复态与修复入口
  - [ ] 团队整体位置被移动、重命名或不可访问时，团队继续显示在首页并标记「需要修复」
  - [ ] 详情提供「重新定位团队」与「移除记录」
  - [ ] 「重新定位团队」只能绑定可识别为同一支有效团队的位置；失败时保留原记录并说明该位置为何不被接受
  - [ ] 「移除记录」只移除应用内失效记录，不删除、移动或修改磁盘文件；执行前必须明确说明这一差异
  - [ ] 任一成员 `AGENT.md` 缺失或不可读、任一成员缺少 slug、同团队内 slug 重复，均使整支团队进入「需要修复」
  - [ ] 需要修复的团队暂时不能用于新建对话，已有会话历史不消失
  - [ ] 失效成员是主 Agent 时，可先指定另一名有效成员再删除失效成员
  - [ ] 文件恢复后重新检查，全部成员有效后解除「需要修复」
  - [ ] 不检查 `AGENT.md` 引用的其他文件是否完整
  - [ ] 覆盖验收 #9
  - 依赖: T4, T5

- [ ] T14: 跨页面异常反馈 + 会话侧约束
  - [ ] 只要至少一支团队「需要修复」，侧边栏「Agent 团队」入口显示一个红点；多支异常不叠加数量
  - [ ] 红点的悬停提示与辅助名称为「有 Agent 团队需要修复」，不只依赖颜色传达含义
  - [ ] 未完成草稿不触发红点
  - [ ] 点击入口后团队首页对应横行必须以「需要修复」明确标出具体异常团队
  - [ ] 已有会话所选团队变为「需要修复」时，历史仍可查看但发送被阻止，会话内团队按钮显示红色异常标识
  - [ ] 会话按钮细节、切换团队与修复后恢复的完整交互留给会话交互 PRD，本任务只落已定义的约束
  - [ ] 覆盖验收 #9 #10
  - 依赖: T13

- [ ] T15: 新建对话团队预选
  - [ ] 记录「上一次成功创建会话时使用的团队」，只有成功创建会话后才更新
  - [ ] 打开、编辑或浏览团队不更新该记录
  - [ ] 新建对话自动预选该团队，用户可在创建前改选
  - [ ] 首次使用且无历史选择时预选软件自带的第一支团队
  - [ ] 上一次使用的团队已删除或处于「需要修复」时回退预选软件自带团队
  - [ ] 不提供可由用户设置的应用级默认团队
  - [ ] 覆盖验收 #8
  - 依赖: T7, T13
