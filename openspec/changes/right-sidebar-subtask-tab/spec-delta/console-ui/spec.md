# console-ui delta：right-sidebar-subtask-tab

本 delta 为 `console-ui` 域新增子任务标签（名 / 成员 / 状态 + 推进内容、内嵌 composer、推进操作、正在查看联动、只读例外边界）的行为规格。**新增** Requirement，不替换既有条目。

> 骨架文件。Requirement 由 codex 在 implement 段按验收 #21（右侧栏只读 + 子任务标签例外提供输入与推进）逐条写入，含 `Source:` 锚点、可判定判据、Scenario。此处不预写正文。
