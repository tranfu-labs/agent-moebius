# 首次启动引导

> 状态：讨论中草案
>
> 所属页面：首次启动引导（独立全屏，非主页面三栏）
>
> 范围：定义新用户第一次打开 agent-moebius 桌面端时的四步引导——环境就绪、选团队、团队接力演示、准备就绪。会话页在此之后承接（见 [主页面会话区](./main-conversation.md)），Agent 团队详细管理见 [Agent 团队](./agent-teams.md)。
>
> 产品基线：[agent-moebius 产品需求文档](../prd.md)
>
> 高保真原型：[onboarding.prototype.html](./onboarding.prototype.html)（浏览器直接打开；它是独立设计交付物，不与正式产品代码共享源码，冲突时以本文为准）
>
> 关联入口：[主页面会话区](./main-conversation.md)、[Agent 团队](./agent-teams.md)

## 页面目标

首启引导不是欢迎，是一次让新用户「信这支团队会替我接力」的最短演示——从环境就绪、看见团队、看它怎么协作，到确认准备开工。

走完引导，用户带走的不是「配置完成」的绿勾，而是**「这支团队会替我接力，我不必当中转站」**的认知。

引导每台电脑只出现一次；走完最后一步落进[主页面会话区](./main-conversation.md)的新建对话形态，此后再打开应用不再重复。

## 用户场景

- 用户下载 agent-moebius 并首次打开，此前用过 codex，还没试过多 Agent 团队协作。
- 用户在同一台电脑上曾完整走完引导，重开应用应直接进主页面，不重复。
- codex 未装或不可用时，用户在第一步就得到具体的下一步指引，而不是退回终端猜为什么装了 App 也开不了工。
- 用户走完引导后自然落入新建对话页，此时项目仍为空——加项目由那页承接，不是引导的职责。

## 入口与去向

### 入口

- 应用首次启动，检测到本机**从未完成过引导**时自动进入。判据待讨论（见「待讨论」）。
- 引导期间侧边栏不出现，主内容区就是引导本身。
- 中途关闭应用后重开，回到引导——具体回到哪一步待讨论。

### 去向

- 走完第 4 步点「开始使用」，落进[主页面会话区](./main-conversation.md)的新建对话形态。
- 落到新建对话页时项目仍为空，由该页承接添加项目。
- 落到新建对话页时**引导中所选定的团队应等同于「上一次成功创建会话时使用的团队」**（见 [agent-teams.md:361](./agent-teams.md)），作为该对话的默认预选团队。

## 页面结构

引导为一屏一步的全屏页，共四步；应用启动即引导，本身不占屏而是一次跳转。

### 应用启动 → 直接进第 1 步

不是屏。应用启动检测到本机从未完成引导时，主窗口直接渲染第 1 步而不是新建对话页。原「首启即落新建对话页」路径被引导替代。

### 第 1 步 · 环境就绪（通过态）

```text
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                                                            │
│                      环境准备                              │
│           agent-moebius 用 codex 来跑每一位成员            │
│                                                            │
│    ┌────────────────────────────────────────────────┐     │
│    │  ✓  codex 已安装                               │     │
│    │  ✓  codex 可以运行                             │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
│                                                            │
│                        [ 继续 ]                            │
│                                                            │
│                                        ● · · ·   1 / 4     │
└────────────────────────────────────────────────────────────┘
```

### 第 1 步 · 环境就绪（codex 缺失态）

```text
┌────────────────────────────────────────────────────────────┐
│                      环境准备                              │
│                                                            │
│    ┌────────────────────────────────────────────────┐     │
│    │  ✕  没有找到 codex                             │     │
│    │      在终端里运行下面这行装好它：              │     │
│    │      ┌──────────────────────────────────┐      │     │
│    │      │  brew install codex        [复制]│      │     │
│    │      └──────────────────────────────────┘      │     │
│    │      装完点下面「重新检查」                    │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
│                     [ 重新检查 ]                           │
│                     ─ 继续 ─  (灰,不可点)                  │
│                                                            │
│                                        ● · · ·   1 / 4     │
└────────────────────────────────────────────────────────────┘
```

第 1 步是**硬门**：环境不通过不能进第 2 步。「继续」在检查通过前始终 disabled；「重新检查」通过后自动放行到「继续」。

### 第 2 步 · 选团队

```text
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                     选择一支团队                           │
│         不同团队处理不同任务，之后你可以随时切换           │
│                                                            │
│    ┌────────────────────────────────────────────────┐     │
│    │ ●  开发团队                            内置    │     │
│    │    开发经理主理 · 开发 · 测试                  │     │
│    │    适合方案、实现、审查、验收类任务            │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
│    ┌────────────────────────────────────────────────┐     │
│    │  ＋ 跟 AI 聊出一支新团队                       │     │
│    │     你说一下要做什么样的活，AI 帮你把成员组齐  │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
│                        [ 继续 ]                            │
│                                                            │
│                                        · ● · ·   2 / 4     │
└────────────────────────────────────────────────────────────┘
```

- 默认选中内置开发团队。正常产品状态下一定至少存在一支软件自带团队（见 [agent-teams.md:386](./agent-teams.md)）。
- 次卡「跟 AI 聊出一支新团队」是**新增的建团队路径**，与 [agent-teams.md:29,334](./agent-teams.md) 里既有的「复制并编辑」是何关系（并列 / 取代 / 独立入口）**待讨论**；本页只钉按钮位与文案。

### 第 3 步 · 团队接力演示

```text
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                 看看他们怎么替你接力                       │
│                                                            │
│    ┌────────────────────────────────────────────────┐     │
│    │        (经理) ──派工──▶ (开发)                 │     │
│    │          ▲                 │                    │     │
│    │          │                 ▼                    │     │
│    │        (收尾) ◀──修正── (测试)                 │     │
│    │                                                │     │
│    │  当前：测试正在核对结果          [重新播放]    │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
│                        [ 继续 ]                            │
│                                                            │
│                                        · · ● ·   3 / 4     │
└────────────────────────────────────────────────────────────┘
```

演示对象**必须是第 2 步所选的团队**，而不是通用的多 Agent 概念动画。默认开发团队演示「开发经理拆解并派工 → 开发执行 → 测试指出问题 → 开发修正 → 测试再次复核通过 → 开发经理带证据收尾」这一条约 10 秒的连续接力。画面突出当前接棒成员，并用方向明确的焦点移动表达控制权交接；已经发生的步骤在同一舞台里留下极简完成记录，问题、修正和复核结果不能随焦点移动而消失。用户可以重新播放，也可以不等动画结束直接继续。减少动态效果模式下不做持续位移，改为逐步淡入和静态高亮，信息与正常模式等价。

### 第 4 步 · 准备就绪

```text
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                                                            │
│                                                            │
│                           ✓                                │
│                                                            │
│                      准备就绪                              │
│           团队已经就位，说出你的目标就能开工               │
│                                                            │
│                                                            │
│                     [ 开始使用 ]                           │
│                                                            │
│                                                            │
│                                                            │
│                                        · · · ●   4 / 4     │
└────────────────────────────────────────────────────────────┘
```

Raycast 式扁平终页：大 ✓ + 标题 + 一句副本 + 单个纯色扁平主按钮，别的都不加。点击落进新建对话页。

## 区域与信息

### 顶部标题区（每屏）

- 主标题 22px semibold，居中，说这一步在干什么。
- 副本 13px sub 色，居中，一句话说清动机；第 4 步的副本可省。

### 主体区（每屏）

- 单栏居中，最大宽度约 `max-w-lg`（≈512px）。
- 第 1 步是检查行组；第 2 步是团队卡组；第 3 步是动画容器；第 4 步是大 ✓。

### 底部操作条（每屏）

- 顶部一条 `border-t border-line` 与主体隔开。
- 左侧步骤点：四个圆点，当前步为 `bg-ink` 实心，其余为 `bg-line-strong`；右侧配 tnum 文字 `n / 4`。
- 右侧主 CTA：「继续」或第 4 步的「开始使用」，`Button size="lg" variant="default"`。
- 第 1 步 codex 缺失态额外显示 `Button variant="outline" size="lg"` 的「重新检查」，位于主 CTA 左侧。

## 操作与反馈

### 前进

- 每步主 CTA 前进到下一步；第 4 步主 CTA 出引导，落到新建对话页。
- 上一步的成果携带到下一步：第 1 步的通过状态、第 2 步的所选团队都对下一步继续有效。
- 高保真单 HTML 中的四步前进和第 4 步「开始使用」全部在同一文件内切换可见状态；最后一步必须直接渲染出新建对话形态并让所选团队肉眼可见，不调用正式应用路由，不以提示框或完成文案代替去向。

### 回退

- 不提供回退。四步都是低复杂度的一次性选择，回退纠正的价值低于线性推进的可预测性。若第 2 步「跟 AI 聊出一支新团队」流程展开后确需回退，另议。

### 中途关闭

应用关闭 = 中断引导。下次打开回到引导的哪一步（从头 vs 从中断处继续），**待讨论**。

### 第 1 步复制安装命令

- codex 缺失时给出的安装命令是可复制的一整行。
- 用户在自己终端粘贴并执行，回到应用点「重新检查」。
- 「复制」按钮成功后的短暂反馈形态待细化。

### 第 3 步重播与继续

- 进入第 3 步后演示自动播放一次；「重新播放」会从第一棒重新开始，不改变已选团队。
- 动画播放期间「继续」始终可用。用户继续后立即结束演示并进入第 4 步，不等待剩余动画。
- 动画播完后停在主 Agent 收尾的完成画面，不自动跳到第 4 步，避免用户还没看清就被带走。

## 页面状态

| 状态 | 页面表现 | 是否可继续 |
| --- | --- | --- |
| 第 1 步 · codex 通过 | 两行绿 ✓ 状态项 | 是 |
| 第 1 步 · codex 缺失 | 红 ✕ 状态项 + 安装命令 + 「重新检查」 | 否 |
| 第 1 步 · codex 装了但跑不起来 | 红 ✕ 状态项 + 排障提示（具体文案待细化） | 否 |
| 第 2 步 · 选中内置团队 | accent 描边 + sel 底 + 蓝色实心圆点 | 是 |
| 第 2 步 · AI 建团队进行中 | 待讨论 | 待讨论 |
| 第 3 步 · 动画播放 | 所选团队成员按接力顺序逐步高亮，当前交棒方向可见，已完成步骤保留极简记录 | 是 |
| 第 3 步 · 动画完成 | 停在测试复核通过、主 Agent 带证据收尾的完成画面，提供「重新播放」 | 是 |
| 第 3 步 · 减少动态效果 | 无持续位移，以逐步淡入和静态高亮表达同一接力顺序 | 是 |
| 第 4 步 · 准备就绪 | 大 ✓ + 单一主按钮 | 是 |

## 待讨论

以下问题已在评审中提出，本版**明确不作答**，不得由实现自行决定：

### 「本机从未完成过引导」的判定

现有 [`desktop/src/console-page/sidebar-preference.ts:31`](../../../desktop/src/console-page/sidebar-preference.ts) 的 `isFirstRunOnboarding` 用「projects 空」判据——加了项目再删光就会重新算首次，不合理。可选：
- 本地 marker 文件（例如 `<dataRoot>/.onboarding-completed`）
- 应用数据库里的 `onboarding_completed_at` 时间戳
- 综合判据（例如两者叠加）

需明确判据后统一改写现有 `isFirstRunOnboarding`。

### 中途关闭后重开

用户走到第 2 步或第 3 步关闭应用，下次打开：
- 从头再走一遍（简单，但可能烦）
- 从中断处继续（第 1 步的通过状态可能已过期需重检、第 2 步的选择需保留）

### 第 2 步「跟 AI 聊出一支新团队」的完整流程

与 [agent-teams.md](./agent-teams.md) 里「复制并编辑」的关系：
- 并列（引导里两种入口共存）
- 取代（未来所有新建团队都走 AI 对话）
- 独立入口（引导专用，agent-teams 页保留复制并编辑）

以及 AI 对话在哪发生：**内嵌引导第 2 步** vs **跳去独立页** vs **跳去 agent-teams 页新流程**。此项独立成流程 PRD 或补进 [agent-teams.md](./agent-teams.md) 更合适。

### 第 4 步是否需要仪式感

当前按 Raycast 式扁平终页承接；曾评估过更 Linear 的方案（不做终页，直接落新建对话页 + 一行完成气泡），已选前者。若走完第 3 步用户信任已建立，第 4 步的仪式感是否冗余，回头看数据再定。

## 关联流程

以下流程将在对应流程 PRD 建立后链接，本文档只记录引导本身的职责：

- 加第一个项目（发生在新建对话页，不在引导里）。
- 「跟 AI 聊出一支新团队」的完整对话流程。
- 中途关闭后的恢复。

## 指标与验收

### 产品指标

- **完成引导 / 打开应用** 的比例（不完成就强关的比例）。
- 从打开 App 到落到新建对话页的中位时长。
- 第 1 步 codex 缺失态触发率（决定 App 下载页是否需要更明确提示先装 codex）。
- 引导完成后 24h 内实际发出第一条对话消息的用户比例。

### 验收标准

1. 本机从未完成引导时打开 App，主窗口进入第 1 步而不是新建对话页。
2. 本机曾完成引导时打开 App，直接进新建对话页，不再看到引导。
3. 第 1 步 codex 缺失时「继续」按钮 disabled；「重新检查」通过后按钮恢复可点。
4. 第 1 步检查项**只查 codex**（是否安装、是否可运行），不检查 gh / GitHub 相关工具，也不检查 claude code 或其他 CLI。
5. 第 2 步默认选中内置开发团队；选中状态由 accent 描边 + sel 底 + 蓝色实心圆点表达。
6. 第 2 步存在「跟 AI 聊出一支新团队」的次要入口按钮；具体行为暂待定但按钮必须存在。
7. 第 3 步是引导的必经步；必须用约 10 秒的连续动画演示第 2 步所选团队从主 Agent 派工、专业成员执行、发现问题、修正、再次复核到主 Agent 带证据收尾的接力过程，不得用静态占位代替。
8. 第 4 步只有一个主 CTA「开始使用」，点击落到新建对话页且已带上第 2 步所选团队作为该对话的默认预选团队。
9. 底部步骤点 1..4 与右侧文字 `n / 4` 同步；当前步为 ink 实心，其余为 line-strong。
10. 所有屏的顶部标题区、底部操作条布局一致；主体最大宽度 `max-w-lg`。
11. 引导期间侧边栏不出现，主内容区就是引导本身；引导完成后恢复主页面三栏布局。
12. 引导中不出现任何 GitHub / gh / PR / issue 相关字样。
13. 所有色值走 [DESIGN.md](../../../packages/console-ui/DESIGN.md) 的令牌（`bg-canvas` `text-ink` `text-pass` 等），无裸 hex。
14. 亮暗双主题下所有屏可读、按钮状态可区分。
15. 第 3 步支持重新播放，播放期间也可继续；减少动态效果模式保留等价信息但不做持续位移。
16. 高保真原型是可离线直接打开的单个 HTML，所有前进操作都在同一文件内切换状态，能够从第 1 步完整走到带有所选团队的新建对话状态，不依赖外部脚本、样式、字体、图片或应用路由；原型与正式产品代码双向不共享源码。

## 现状参考与产品缺口

现有实现里没有独立的欢迎 / 引导界面——冷启动直接渲染三栏操作台的新建对话页，项目为空时只有一行小字「还没有项目，从上面的项目按钮添加一个」；环境自检（codex/gh）完全不阻断、也不在主窗口显示。

相较本文定义，当前实现的主要缺口：

- **无引导页**：无任何一步；主窗口直接进新建对话页。
- **环境自检沉默**：[`desktop/src/env-doctor.ts`](../../../desktop/src/env-doctor.ts) 检查 codex / gh / gh auth，结果只进独立诊断页；主窗口对 codex 缺失零提示，用户要发消息才以失败 run 暴露。
- **多余的 gh 检查**：`env-doctor.ts` 现同时查 `gh --version` 和 `gh auth status`；按本文与项目决定（本项目 desktop-only，不做 GitHub 侧执行），这两项应清除，只保留 codex 相关。
- **`isFirstRunOnboarding` 判据不稳**：[`sidebar-preference.ts:31-35`](../../../desktop/src/console-page/sidebar-preference.ts) 用「projects 空」当首次，加删项目会反复触发；本文需要稳定的「本机曾完成引导」判据。
- **没有 AI 建团队路径**：Agent 团队页现只有「复制并编辑」；本文引导里承诺的「跟 AI 聊出一支新团队」是新能力。
- **新建对话页缺「已带上引导中选定团队」的传参链路**：现在冷启动预选团队走 [agent-teams.md:367](./agent-teams.md) 「首次预选软件自带第一支」，本文需要它变成「引导中选定的团队」。

## 非目标

本引导明确不做：

- codex 之外的任何 CLI 检查（gh / claude code / node 等）。
- 让用户在引导里编辑内置团队的成员或 `AGENT.md`（属于 [Agent 团队](./agent-teams.md)页的职责）。
- 添加第一个项目（属于[主页面会话区](./main-conversation.md)新建对话形态的职责）。
- 授权 / 隐私声明 / 用户协议弹层（若产品需要，另议）。
- 教用户完整用法——那是官网 / 文档的职责，引导只做「让他信这支团队会接力」这一件事。
- 演示通用的「什么是多 Agent」抽象概念——第 3 步演的是用户刚选的**具体这一支团队**，不做概念铺垫。
